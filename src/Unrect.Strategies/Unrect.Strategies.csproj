<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- Two things are shared: AnchorNotFoundException.Description, which ShapeEngine renders, and
         CellMatching, so Caption, Field and the matchers agree on what matching means. (Fields once
         reached for the Predicate*Landmark classes too; RowWhere/ColumnWhere's described overloads
         replaced that.) -->
    <InternalsVisibleTo Include="Unrect" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Unrect.Core\Unrect.Core.csproj" />
  </ItemGroup>

</Project>
